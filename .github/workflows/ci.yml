name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  lint:
    name: Python correctness lint
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7

      # Runs before the image build so a broken name or import fails in
      # seconds rather than after a full multi-stage Docker build.
      - name: Check correctness rules
        uses: astral-sh/ruff-action@v3
        with:
          version: "0.16.4"
          args: check --output-format github

  test:
    name: Python, JavaScript and browser regressions
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 20
          cache: npm

      - name: Install browser test dependencies
        run: npm ci

      # The runner image carries Google's Chrome package repository, and
      # installing Playwright's dependencies updates every source apt knows
      # about. When Google serves an index that disagrees with their own
      # release file, apt fails the whole update and the browser install stops
      # before a single test has run. The browser these tests drive is
      # Playwright's own Chromium build, downloaded separately, so that
      # repository is never wanted here and a run should not depend on it.
      - name: Drop the unused Google Chrome package source
        run: |
          sudo grep -rl 'dl\.google\.com' /etc/apt/sources.list.d/ 2>/dev/null \
            | sudo xargs -r rm -f

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Build application image
        run: docker build --tag atari-file-forge:ci .

      - name: Run Python tests in the application image
        run: >-
          docker run --rm
          --volume "${{ github.workspace }}:/source"
          --workdir /source
          atari-file-forge:ci
          python -m unittest discover -s tests -v

      - name: Run generated-media performance smoke profile
        run: >-
          docker run --rm
          --volume "${{ github.workspace }}:/source"
          --workdir /source
          atari-file-forge:ci
          python -m tools.benchmark_media --profile quick --output output/benchmark-ci.json

      - name: Run JavaScript unit tests
        run: node tests/run_js_tests.js

      - name: Start application for browser tests
        run: >-
          docker run --detach --name atari-file-forge-ci
          --publish 8666:8666
          atari-file-forge:ci

      - name: Wait for the application
        run: |
          for attempt in $(seq 1 60); do
            if curl --fail --silent http://127.0.0.1:8666/api/health >/dev/null; then
              exit 0
            fi
            sleep 1
          done
          docker logs atari-file-forge-ci
          exit 1

      - name: Run browser regressions
        run: npm run test:browser

      - name: Show application log on failure
        if: failure()
        run: docker logs atari-file-forge-ci

      - name: Stop browser-test application
        if: always()
        run: docker rm --force atari-file-forge-ci || true

  multiarch:
    name: Build ${{ matrix.platform }}
    runs-on: ubuntu-latest
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux/amd64
            tag: amd64
          - platform: linux/arm64
            tag: arm64
          - platform: linux/arm/v7
            tag: armv7
    steps:
      - uses: actions/checkout@v7

      - uses: docker/setup-qemu-action@v4

      - uses: docker/setup-buildx-action@v4

      # Docker Hub answers the HEAD request for the base image's manifest
      # with a 502 often enough to fail a run that has nothing wrong with it,
      # and a failed run on main is a mail in somebody's inbox about a
      # registry hiccup. The first attempt is allowed to fail so a second can
      # decide the job; when the registry is healthy the retry never runs and
      # this costs nothing.
      - name: Build without publishing
        id: build
        continue-on-error: true
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: ${{ matrix.platform }}
          push: false
          load: true
          tags: atari-file-forge:matrix-${{ matrix.tag }}
          cache-from: type=gha,scope=${{ matrix.platform }}
          cache-to: type=gha,mode=max,scope=${{ matrix.platform }}

      - name: Build without publishing, second attempt
        if: steps.build.outcome == 'failure'
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: ${{ matrix.platform }}
          push: false
          load: true
          tags: atari-file-forge:matrix-${{ matrix.tag }}
          cache-from: type=gha,scope=${{ matrix.platform }}
          cache-to: type=gha,mode=max,scope=${{ matrix.platform }}

      - name: Verify architecture-native runtime
        run: |
          mkdir -p output/release-evidence
          docker run --rm \
            --volume "${{ github.workspace }}:/source" \
            --workdir /source \
            atari-file-forge:matrix-${{ matrix.tag }} \
            sh -lc 'set -eu; uname -m; test -x /usr/local/bin/hxcfe; test -x /usr/bin/hatari; python -c "import tempfile; from pathlib import Path; import flask; import atarinut.filesystem; import atarinut.tosrom; from capstone import CS_ARCH_M68K, CS_MODE_M68K_000, Cs; from app.server import create_app; Cs(CS_ARCH_M68K, CS_MODE_M68K_000); work = Path(tempfile.mkdtemp()); app = create_app(work_dir=work); client = app.test_client(); response = client.get(\"/api/health\"); assert response.status_code == 200, response.get_data(as_text=True); print(\"Architecture-native runtime gate passed\")"' \
            2>&1 | tee output/release-evidence/${{ matrix.tag }}.log

      - name: Verify service health on the target architecture
        run: |
          container="atari-file-forge-${{ matrix.tag }}"
          docker run --detach --name "$container" --publish 8666:8666 atari-file-forge:matrix-${{ matrix.tag }}
          for attempt in $(seq 1 90); do
            if curl --fail --silent http://127.0.0.1:8666/api/health | tee output/release-evidence/${{ matrix.tag }}-health.json; then
              docker rm --force "$container"
              exit 0
            fi
            sleep 1
          done
          docker logs "$container" | tee -a output/release-evidence/${{ matrix.tag }}.log
          docker rm --force "$container"
          exit 1

      - name: Retain architecture evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: release-evidence-${{ matrix.tag }}
          path: output/release-evidence/
          if-no-files-found: warn
